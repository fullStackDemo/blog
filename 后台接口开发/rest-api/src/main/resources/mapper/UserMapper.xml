<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zz.mapper.UserMapper">

    <resultMap id="BaseResult" type="com.zz.entity.User">
        <id column="user_id" property="userId"></id>
        <id column="user_name" property="userName"></id>
    </resultMap>

    <sql id="base">
        user_id,
        user_name
    </sql>

    <!-- 查询所有user -->
    <select id="findAllUser" parameterType="com.zz.query.UserQuery" resultMap="BaseResult">
        select
        <include refid="base" />
        from user
    </select>

    <!-- 查询user -->
    <select id="findUserById" parameterType="com.zz.query.UserQuery" resultMap="BaseResult">
        select
        <include refid="base"/>
        from user
        where user_id = #{userId}
    </select>

    <select id="findUserByName" parameterType="com.zz.query.UserQuery" resultMap="BaseResult">
        select
        <include refid="base"/>
        from user
        where user_name = #{userName}
    </select>

    <insert id="insert">
        INSERT INTO user(
        user_name,
        password
        ) VALUES (
        #{userName},
        #{password}
        )
    </insert>


</mapper>
